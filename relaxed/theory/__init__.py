"""Theory sandbox for soft equivariance (REMUL bias-variance account).

This package holds the *exact-regime* falsification gate (Phase 0) for the
research program "When and Which Symmetry to Enforce". Everything here is a
self-contained linear-Gaussian model with orthogonal group representations,
where the REMUL equivariance penalty, its optimal weight, the hard/unconstrained
crossover, and loss-side group selection are all analytically available and can
be checked against Monte-Carlo simulation to tolerance.

Entry point: ``python -m relaxed.theory.sandbox``.
"""
