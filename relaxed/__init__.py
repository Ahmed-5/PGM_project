"""relaxed — unified framework for relaxed/approximate equivariance experiments.

Merges the former ``remul`` package (paper reproduction: dynamics with the
ground-truth-anchored multitask objective) and the former top-level pipeline
(layer-wise functional equivariance loss on graph regression) behind a single
config, CLI, loss library, model/dataset registry, and run-reporting schema.
"""
